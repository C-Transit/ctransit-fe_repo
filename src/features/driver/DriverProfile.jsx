import ComingSoon from '../agent/ComingSoon';

export default function DriverProfile() {
  return (
    <ComingSoon
      title="Driver Profile & Credentials"
      description="Driver license verification, commercial permit status, emergency contacts, and assigned transport zone."
      progress={20}
    />
  );
}
