import ComingSoon from '../agent/ComingSoon';

export default function DriverTrips() {
  return (
    <ComingSoon
      title="Driver Trips & Routes"
      description="Active trip scheduling, passenger manifests, campus route navigation, and bus stop checkpoints."
      progress={15}
    />
  );
}
