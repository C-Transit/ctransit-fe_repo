/**
 * Root Application Module.
 * Target application entry is resolved at build/dev time by Vite (vite.config.js)
 * according to VITE_APP_TARGET (web | admin | agent).
 */
export { default } from './apps/web/App';

